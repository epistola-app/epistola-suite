# Production stdout is newline-delimited Logstash JSON, matching the suite application.
logging:
  structured:
    format:
      console: logstash
    json:
      customizer: app.epistola.suite.logs.EpistolaStructuredLoggingJsonCustomizer
      stacktrace:
        max-length: 32768
