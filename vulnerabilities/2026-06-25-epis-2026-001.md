---
{
  "schema_version": "1.6.0",
  "id": "EPIS-2026-001",
  "published": "2026-06-25T16:18:42Z",
  "modified": "2026-08-22T00:00:00Z",
  "summary": "Stored HTML injection in editor resource pickers",
  "aliases": [],
  "affected": [
    {
      "package": {
        "ecosystem": "GIT",
        "name": "github.com/epistola-app/epistola-suite",
        "purl": "pkg:github/epistola-app/epistola-suite"
      },
      "ranges": [
        {
          "type": "GIT",
          "repo": "https://github.com/epistola-app/epistola-suite.git",
          "events": [
            { "introduced": "32177aa1271371eb10694cc860b0d6e5972fba19" },
            { "fixed": "2d94d6ce22df0e021f6bf958f17ab2016e82dfaa" }
          ]
        },
        {
          "type": "SEMVER",
          "events": [{ "introduced": "0.0.0" }, { "last_affected": "1.0.1" }]
        }
      ],
      "ecosystem_specific": {
        "affected_components": {
          "asset_picker": "v0.0.0 through v1.0.1",
          "stencil_picker": "v0.12.0 through v1.0.1"
        }
      }
    }
  ],
  "references": [
    { "type": "REPORT", "url": "https://github.com/epistola-app/epistola-suite/issues/644" },
    {
      "type": "FIX",
      "url": "https://github.com/epistola-app/epistola-suite/commit/4f7ae06920c82ebff2124b651a7d2a48d159aee7"
    },
    {
      "type": "FIX",
      "url": "https://github.com/epistola-app/epistola-suite/commit/2d94d6ce22df0e021f6bf958f17ab2016e82dfaa"
    }
  ],
  "database_specific": {
    "severity": "HIGH",
    "status": "fixed-in-source",
    "cwe_ids": ["CWE-79"],
    "demonstrated_xss_exposure": "v0.0.0 through v0.26.0, and v1.0.0-RC1",
    "patched_release": null,
    "github": {
      "sync": true,
      "state": "draft",
      "package": { "ecosystem": "other", "name": "Epistola Suite" },
      "vulnerable_version_range": ">= 0.0.0, <= 1.0.1",
      "patched_versions": null,
      "vulnerable_functions": ["openAssetPickerDialog", "openStencilPickerDialog"]
    }
  }
}
---

# Stored HTML injection in editor resource pickers

Asset names derived from uploaded filenames were interpolated into the asset picker's
`innerHTML`. Stencil names, descriptions, and tags were later found to use the same unsafe
rendering pattern. An authenticated user able to create one of these resources could store HTML
that was rendered when another user opened the corresponding picker.

## Impact

Through `v0.26.0`, and in `v1.0.0-RC1`, the application's Content Security Policy allowed inline
scripts and event handlers. A payload using an injected element with an event handler could
therefore execute in a victim's authenticated editor session. This required the victim to open the
affected picker.

`v1.0.0-RC2` removed `unsafe-inline` from `script-src`; every GA release has shipped with that
strict policy. It blocks the demonstrated event-handler payload and materially mitigates script
execution. Those releases remain in the affected-code range because they continued to create
arbitrary HTML from stored values.

## Resolution

The source fix constructs asset cards with DOM APIs and assigns author-controlled values through
safe properties and `textContent`. It escapes author-controlled stencil text before the remaining
HTML template interpolation. Command-boundary validation also rejects markup delimiters and
control characters in asset names, stencil names, and stencil tags as defense in depth.

Until a patched release is available, operators should retain the shipped strict Content Security
Policy and avoid weakening `script-src`. Once the fix is released, this record must be updated with
the first patched version and its GHSA/CVE alias, if assigned.
